# MiddMarketPlace

Setting up the project: 

- Init github repository
- Install dependencies: mongoose, request, express, express validator, jsonwebtoken, bcryptjs
- install dev dependencies: npm i -D nodemon concurrently
- Set up the database (MongoDb) using Atlas
